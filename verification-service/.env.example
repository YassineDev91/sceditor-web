# Copy this file to verification-service/.env and fill in a real secret.
# verification-service/.env is gitignored (matches the existing ".env" pattern
# in .gitignore) and must never be committed.

# Shared secret the frontend must send as the X-Verify-Secret header.
VERIFY_SHARED_SECRET=change-me

PORT=4100
